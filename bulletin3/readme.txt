1:grunt 半自动
在此文件夹运行grunt ，
可以自动把requirejs 以来的文件打包成一个文件，并把以来的css也一起打包放dist目录下。

但是不包含requirejs
不足的地方：
2：css 没有压缩
3：requirejs 没有压缩到一起。

在压缩后 backbone 的routes 被重写，
但是直接访问还可以， 
服务器重写了,排除，因为同样的服务器，源码可以，压缩的不行。
还是其他的。
压缩的时候导致代码出现问题？
1：不压缩，只合并，看会不会出现问题
2：不压缩某些部分，看能不能出问题。